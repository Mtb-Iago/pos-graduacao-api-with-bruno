version: "3.9"

services:
  node-app:
    image: node:22
    container_name: node-app
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "3000:3000"
    command: sh -c "npm install && npm run dev"