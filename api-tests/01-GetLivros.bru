meta {
  name: Listar Livros
  type: http
  seq: 1
}

get {
  url: http://localhost:8000/livros
  body: none
}

tests {
  test("Deve retornar status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("Deve retornar um array de livros", function() {
    expect(res.getBody()).to.be.an('array');
  });
}