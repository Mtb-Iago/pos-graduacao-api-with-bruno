meta {
  name: Buscar Livro Criado
  type: http
  seq: 2
}
get {
  url: http://localhost:8000/livros/10
}
tests {
  test("Deve encontrar o livro de ID 10", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().nome).to.equal("Livro de Teste Automatizado");
  });
}