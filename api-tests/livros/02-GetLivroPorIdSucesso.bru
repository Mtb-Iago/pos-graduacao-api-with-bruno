meta {
  name: Listar Livro
  type: http
  seq: 2
}

get {
  url: http://localhost:8000/livros/1
  body: none
}

tests {
    test("Deve retornar o livro específico", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().id).to.equal("1");
 });
}