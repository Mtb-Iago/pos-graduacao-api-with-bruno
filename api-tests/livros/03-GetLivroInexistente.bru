meta {
  name: Buscar ID Inexistente
  type: http
  seq: 3
}
get {
  url: http://localhost:8000/livros/9999
}
tests {
  test("Deve retornar 404 para número que não existe", function() {
    expect(res.getStatus()).to.equal(404);
  });
}