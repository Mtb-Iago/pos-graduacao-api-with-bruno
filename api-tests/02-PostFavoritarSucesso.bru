meta {
  name: Favoritar Livro Sucesso
  type: http
  seq: 2
}

post {
  url: http://localhost:8000/favoritos/3
  body: none
}

tests {
  test("Deve retornar status 201 ou 200", function() {
    expect([200, 201]).to.include(res.getStatus());
  });

  test("Deve confirmar o favoritismo", function() {
    expect(res.getBody()).to.contain("sucesso");
  });
}