meta {
  name: Favoritar Inexistente
  type: http
  seq: 3
}

post {
  url: http://localhost:8000/favoritos/999
  body: none
  auth: none
}

tests {
  test("Deve retornar 404 para livro inexistente", function() {
    expect(res.getStatus()).to.equal(404);
  });
}